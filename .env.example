# Bluematador MCP Server Configuration
#
# For HTTP/Remote mode, you can optionally set default credentials
# that will be used if the client doesn't provide them.
#
# Security Note: If deploying publicly, DO NOT set these values.
# Instead, require clients to provide credentials per request.

# Optional: Default Bluematador API credentials
# BLUEMATADOR_API_KEY=your-api-key-here
# BLUEMATADOR_ACCOUNT_ID=your-account-id-here

# Optional: Custom Bluematador API base URL
# BLUEMATADOR_BASE_URL=https://app.bluematador.com

# HTTP Server Configuration
PORT=3000

# Node Environment
NODE_ENV=production
